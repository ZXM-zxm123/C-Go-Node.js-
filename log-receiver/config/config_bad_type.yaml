# Bad type example
cpp_receiver:
  udp_port: "not a number"  # Should be integer
  tcp_port: 515
  queue_size: 100000
  redis_host: "localhost"
  redis_port: 6379
  redis_stream: "log_stream"

go_consumer:
  redis_host: "localhost"
  redis_port: 6379
  redis_stream: "log_stream"
  consumer_group: "log_consumers"
  consumer_name: "consumer_1"
  metrics_interval: 60
  grpc_port: 50051
