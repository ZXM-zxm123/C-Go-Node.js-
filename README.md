# C-Go-Node.js-
使用 C++ 编写高性能日志接收器（支持 UDP/TCP），将原始日志写入零拷贝队列。Go 服务消费队列，进行日志解析、正则过滤、字段提取，并计算聚合指标（如每分钟错误数）。Node.js 提供 REST API 查询日志和指标，并实现简单的 Web 控制台（可选）。三服务通过 gRPC 或消息队列（如 Redis Streams）通信。实现告警规则管理（例如“10分钟内 error 超过 5 次触发 Webhook”）。输出三端代码及配置。
